// Type stub for classic (`Node`) moduleResolution, which ignores the package
// `exports` map and resolves the `@stillpointlab/md-editor/markdown` subpath by
// file path. The host server build uses `moduleResolution: Node`; bundler-based
// builds resolve via `exports` and ignore this file.
export * from './dist/markdown';
